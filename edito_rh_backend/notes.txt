-create virtual env : virtualenv -p python3 venv
-then activate it with: venv\Scripts\activate
-pip list

-generate requirement: pip freeze > requirements.txt
-create venv from requirement: 
virtualenv -p python3 venv
venv\Scripts\activate
-make vs code with venv
palette => >python select interpreter => add path to venv/scripts/python.exe
pip install -r requirements.txt
----------------------------------
-django-admin startproject edito_rh_frontend
-python manage.py runserver
--------------settings-----------------
-Secret key: every django project have a secret key
-installed apps
---------------apps-----------------
-python manage.py startapp fonctions
-in your app add urls.py
-django can be server side rendering , templates
----------------------------------
-Django can do either, but it is designed to render on the server side in its default 
configuration, not the client side. To use server side rendering, use Django as intended 
and according to the many tutorials out there. To use client side rendering with something 
like React or Vue, use the Django Rest Framework.
----------------------------------
-pip install djangorestframework
in installed_apps add
"rest_framework",

-django https://www.youtube.com/watch?v=OTmQOjsl0eg
-django rest framework 
----------------serializers-------------------
-before sending data to client we serialize it to json
-python manage.py migrate
sync the django project with the db 
-python manage.py createsuperuser
superuser
username:edito
password:groupe_edito@2022
------------connect database to django------------
-mysql 
pip install pymysql



- python manage.py inspectdb > models.py
-python manage.py makemigrations
-python manage.py migrate
-python manage.py migrate --database=users
-
----------------------------------------
from rest_framework.views import APIView

generic views and mixins
----------------------------------------
-create folder apps
-cd apps
-django-admin startapp fonctions
add this in installed apps setting
'apps.app_1'

-----------------------------
query doc
https://docs.djangoproject.com/en/4.0/topics/db/queries/

---filter or 
from django.db.models import Q
q1 = User.objects.filter(Q(income__gte=5000) | Q(income__isnull=True))
-
q2 = User.objects.filter(income__gte=5000) | User.objects.filter(income__isnull=True)

--filter and

.filter(somethin).filter(something_else)
.filter(entry__headline__contains='Lennon', entry__pub_date__year=2008)
-----------------------------
metadata
https://johnfraney.ca/blog/typescript-synchronize-django-rest-framework-vue-js/

-----------------------------

python manage.py migrate --database=users_db
python manage.py migrate --database=main_db
python manage.py createsuperuser --database=users_db

user1@gmail.com

manage.py makemigrations (when you add a model)

if prb happens
leave only init in migrations then makemigrations then migrate

-----------------------------
pip install PyJWT
pip install django-cors-headers

-----------------------------
error:{
"code":200,
"status":,
"message":"human readable error",
}

Facebook https://www.baeldung.com/rest-api-error-handling-best-practices
{
    "error": {
        "message": "Missing redirect_uri parameter.",
        "type": "OAuthException",
        "code": 191,
        "fbtrace_id": "AWswcVwbcqfgrSgjG80MtqJ"
    }
}
ID: Something that will let your consumers contact you. Then you can reference your logs and metrics to find out what went wrong.
CODE: Even though the HTTP code comes back on the header, it’s good practice to add it to the body as well. This lets others see it and easily map it to a model.
STATUS: This is the text of what your status code means. Don’t make your consumers look it up.
TITLE: Let the consumer know what caused the client error here, but don’t share too much info about internal server errors.


mark=Employe
student=Fonction